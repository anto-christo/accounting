branches:
  only:
  - build-test

dist: xenial

language: node_js
node_js:
  - "8.12.0"

install:
  - yarn global add https://github.com/anto-christo/frappejs-cli#production-build
  - yarn

script:
  - frappe build electron
  - yarn dist